#!/bin/groovy
pipeline {
    agent {
        docker {
            image 'fedora'
            args '-u 0'
        }
    }

    stages {
        stage('Init') {
            steps {
                copyArtifacts(projectName: 'mm')
                sh('dnf install -y hello-[^d]*rpm')
                sh('hello')
            }
        }
    }
}
